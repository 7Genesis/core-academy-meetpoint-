resources:
  - ../../base

patches:
  - target:
      kind: HorizontalPodAutoscaler
      name: meetpoint-api
    patch: |-
      - op: replace
        path: /spec/minReplicas
        value: 3
      - op: replace
        path: /spec/maxReplicas
        value: 12
